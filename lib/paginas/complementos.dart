import 'package:flutter/material.dart';

class Complemento1 extends StatelessWidget {
  const Complemento1({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/PLAZA0.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento2 extends StatelessWidget {
  const Complemento2({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/EJECUTIVO.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento3 extends StatelessWidget {
  const Complemento3({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/PASEO.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento4 extends StatelessWidget {
  const Complemento4({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/TEATRO.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento5 extends StatelessWidget {
  const Complemento5({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/BIBLIOTECA.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento6 extends StatelessWidget {
  const Complemento6({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/FUENTE.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento7 extends StatelessWidget {
  const Complemento7({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/PLAZAzaragoza.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento8 extends StatelessWidget {
  const Complemento8({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/Fundidora.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento9 extends StatelessWidget {
  const Complemento9({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/Corredor.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento10 extends StatelessWidget {
  const Complemento10({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/ParqueEspana.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Complemento11 extends StatelessWidget {
  const Complemento11({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/Barrio.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Evento1 extends StatelessWidget {
  const Evento1({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/evento0.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Evento2 extends StatelessWidget {
  const Evento2({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/evento2.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Evento3 extends StatelessWidget {
  const Evento3({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/evento1.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Evento4 extends StatelessWidget {
  const Evento4({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/evento4.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Evento5 extends StatelessWidget {
  const Evento5({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class Gratis1 extends StatelessWidget {
  const Gratis1({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/Gratis1.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Gratis2 extends StatelessWidget {
  const Gratis2({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/Gratis1.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Gratis3 extends StatelessWidget {
  const Gratis3({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class Gratis4 extends StatelessWidget {
  const Gratis4({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class Gratis5 extends StatelessWidget {
  const Gratis5({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class Costo1 extends StatelessWidget {
  const Costo1({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/costos/Horno3.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Costo2 extends StatelessWidget {
  const Costo2({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/costos/COMIDArio.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Costo3 extends StatelessWidget {
  const Costo3({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/costos/HOTELmacroplaza.png.png"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}

class Costo4 extends StatelessWidget {
  const Costo4({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 180,
        decoration: BoxDecoration(
          color: Colors.amber,
          borderRadius: BorderRadius.circular(
            20,
          ),
          border: Border.all(width: 2, color: Colors.deepPurple),
          image: const DecorationImage(
            image: AssetImage("src/costos/SANTALUCIA.jpg"),
            fit: BoxFit.cover,
          ),
        ),
      ),
    );
  }
}
